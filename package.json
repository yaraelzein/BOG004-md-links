{
  "name": "md-links",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": ">=14.x"
  },
  "scripts": {
    "test": "jest --verbose --coverage"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
